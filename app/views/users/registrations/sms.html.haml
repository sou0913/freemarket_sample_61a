-# ページタイトル
- content_for(:title) { '会員情報入力 - メルカリ サンプル' }
.devise-main__registration
  %h2.devise-main__registration__head 電話番号の確認

  = form_for @user, url: new_address_users_path, method: :get, html: { class: 'devise-main__registration__form' } do |f|
    .field
      = f.label "携帯電話の番号"
      %span.necessary 必須
      = f.text_field :phone_number, placeholder: "携帯電話の番号を入力"

    %h2.describe 本人確認のため、携帯電話のSMS(ショートメッセージサービス)を利用して認証を行います。
    .actions
      = f.submit "SMSを送信する",class:"btn-default btn-red"
    %h2.describe ※電話番号は本人確認や不正利用防止のために利用します。他のユーザーに公開されることはありません。
    %p.reason
      = link_to "https://www.mercari.com/jp/help_center/article/244/" do
        電話番号の確認が必要な理由>