.signup-main
  .header-signup
    .signup-container
      %h2.signup-container__head
        支払い方法
      = form_with(model: @card, url: create_user_registration_path, html:{name: "inputForm"}, method: :post, local: true) do |f|
        = render partial: "cards/card_form", locals:{f: f}